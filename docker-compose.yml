services:
  bot-en:
    build: .
    env_file: .env.en
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  bot-ja:
    build: .
    env_file: .env.ja
    volumes:
      - ./data:/app/data
    restart: unless-stopped
